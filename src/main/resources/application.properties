# Honor the X-Forwarded-* headers sent by a reverse proxy (e.g. GitHub
# Codespaces port forwarding). Without this, redirects after a form submit
# jump to localhost:8080 instead of staying on the public forwarded URL.
server.forward-headers-strategy=framework
